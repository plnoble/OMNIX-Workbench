# Development Worklog

## 2026-06-23

- Finalized the product naming hierarchy: `OMNIX Workbench` for product/window/installer/docs, `OMNIX` for compact UI, `多 Agent 开发与协作工作台` as the Chinese descriptor, and `omnix-workbench` for package/crate/binary artifacts.
- Updated Tauri product metadata and identifier, Node/Cargo package names, Rust library crate, web title, license, README, user-visible dock/onboarding copy, and source headers.
- Centralized frontend brand constants in `src/lib/constants.ts`.
- Removed the obsolete unreachable `WorkbenchTab`; renamed the backend `workbench` command domain and run-schema helpers to neutral `runs` naming.
- Added a permanent naming standard to `AGENTS.md` and recorded the architecture decision in `decisions.md`.
- Preserved the existing SQLite location (`~/.omnix/omnix.db`) and stable WiX upgrade code so the identity rename does not reset user data or MSI upgrade continuity.
- Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`, `cargo check`, formal MSI build outside the sandbox, and NSIS build.
- Produced `omnix-workbench.exe`, `OMNIX Workbench_0.1.0_x64_en-US.msi`, and `OMNIX Workbench_0.1.0_x64-setup.exe`; Windows exe metadata reports `OMNIX Workbench`.
- Confirmed current source/configuration contains no retired product/package names and parsed all 19 changed JSON files as UTF-8.
- Renamed the GitHub repository from `plnoble/OMNIX-Development-Tools` to `plnoble/OMNIX-Workbench`, updated local `origin`, and refreshed README clone instructions plus the permanent naming contract.
- Applied the user-selected provisional E1 icon: generated an isolated O+X master, removed the chroma-key background, regenerated all 53 Tauri icons, replaced favicon/header/status-dock branding, and removed Vite/React placeholder assets.
- Detected that the first rebuilt exe still contained the old icon because Cargo reused `resource.lib`; added icon/config `rerun-if-changed` rules to `build.rs`, rebuilt, and verified E1 by parsing PE icon resources directly.
- Added explicit NSIS installer/uninstaller icon configuration and verified the rebuilt NSIS embeds E1.
- MSI rebuild was not run because non-sandbox approval was rejected after the Codex usage allowance was exhausted; the existing MSI is marked stale for this icon slice.

## 2026-06-24

- Resumed the pending E1 MSI task after non-sandbox approval became available.
- `npm.cmd run tauri:build:msi` passed normal WiX ICE validation and generated `OMNIX Workbench_0.1.0_x64_en-US.msi` (11,825,152 bytes, 2026-06-24 08:47:51).
- Decompiled the MSI with WiX `dark.exe`; verified the packaged executable and MSI `ProductIcon` both match the E1 master with approximately 0.248 resize-level image difference.
- Confirmed stable WiX upgrade code `1339290b-b4f1-5e65-b4b9-f8b0141ebc54` and removed temporary verification directories.

## 2026-06-12

- Initialized the agent collaboration protocol for OMNIX Workbench (the local checkout folder still used its historical name at this point).
- Started implementation of the multi-agent workbench refactor. Agreed product direction: Workbench-led all-in-one AI client with Agent + Team + Skill as the main spine, resources as support, and unfinished features visible under Labs.
- Added Workbench backend schema and commands for `WorkspaceRun`, `TeamPlan`, `AgentRun`, and visible `LabFeature` registry.
- Added frontend Workbench and Labs tabs, made Workbench the default entry, merged chat into the Workbench screen, and reshaped navigation into Core / Resources / Labs.
- Added typed frontend Workbench/Labs API wrappers and shared types for runs, assignments, skill packages, sync targets, labs, and resource bindings.
- Added OpenCode to the primary skill-sync adapter registry.
- Fixed `prompt_guard` data-exfil detection for "show me your system prompt".
- Isolated four slow/manual Rust tests with explicit `#[ignore]` reasons so `cargo test --lib` can be a usable baseline.
- Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`, `cargo check`, `cargo test --lib`, and preview visual smoke test.
- Started second refactor slice after AionUi and Cherry Studio review. User feedback rejected the visible Workbench pile-up and requested a cleaner first screen, separate single-agent Work and Team flows, configurable fixed-vs-launcher navigation, a Cherry-like Models center, optional Quick Assistant, and optional multi-knowledge-base RAG for ordinary chat only.
- Completed second refactor slice:
  - Default route changed to `工作`; old `chat` and `workbench` navigation requests normalize to the new single-agent Work entry.
  - Added configurable top-bar/application launcher layout with persistent `ui.navigation.layout`, including fixed, launcher, hidden, and restore-default placements.
  - Reworked Work page into Agent selector plus large auto-growing input, model selector, permission mode, work mode, optional search, optional Knowledge binding for ordinary chat only, and a collapsible workspace panel.
  - Split Models into a focused app page and removed the visible model platform tab from Settings.
  - Added Search and Quick Assistant app pages so those features can live in the launcher instead of the main workflow.
  - Rewrote Agents into a card-based local agent page with detection, install/update actions, version/path display, start-work flow, and model binding drawer.
  - Fixed custom model saving by requiring a selected provider and using provider-bound stable IDs.
  - Updated backend model import IDs and API-key usage so encrypted active keys are decrypted before model fetch and health checks.
  - Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`, and `cargo check`.
  - Visual preview could not be kept alive in this host; Playwright is not installed, so visual QA remains a manual follow-up.
- Applied user UI feedback after desktop testing:
  - Narrower windows no longer show the right workspace panel at `lg`; it now waits until `xl` and uses a smaller width before `2xl`.
  - Left contextual sidebar now shrinks from `w-72` to `w-60` on narrower desktop widths.
  - Work mode changed from a flat segmented control plus duplicate label to a compact dropdown matching the permission selector style.
  - Verification passed: `npx.cmd tsc --noEmit` and `npm.cmd run build`.
- Applied second user UI feedback patch:
  - Removed `Settings` and `Diagnostics` from the configurable application registry because they are fixed title-bar controls.
  - Hid the duplicate Settings theme selector; theme switching now lives only in the title bar.
  - Clarified launcher semantics in the UI: fixed = title bar, collected = app grid, hidden = hidden recovery area.
  - Fixed navigation placement persistence to use the latest layout state, preventing stale clicks from moving another entry unexpectedly.
  - Added front/back ordering controls for title-bar fixed entries.
  - Reworked `SkillHub` height and scroll boundaries so skill content, topology, marketplace, and scan panels no longer overlap in smaller desktop windows.
  - Verification passed: `npx.cmd tsc --noEmit` and `npm.cmd run build`.

## 2026-06-13

- Applied desktop shell consistency and model-center bug patch after user testing:
  - Moved `AppHeader` to the top-level app chrome so title-bar navigation, theme, settings, diagnostics, and preview controls keep a stable position across Work, Agents, Skills, Models, and other pages.
  - Hid the contextual left sidebar outside Work/Team surfaces, so Agents and resource pages no longer show a misleading "work context" rail.
  - Reworked the shared dialog component to follow theme tokens instead of hard-coded dark/accent styling.
  - Reworked workspace selection to use a Windows folder picker command instead of asking the user to manually type a path.
  - Added `pick_directory` Tauri command and `shellApi.pickDirectory()` wrapper for the workspace modal.
  - Fixed Ark/Volces model fetching so Volcano-style API addresses do not fall back to Anthropic/Claude known-model defaults; successful fetch/import now replaces stale model rows for that provider.
  - Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`, and `cargo check`.
- Started third refactor slice:
  - Scope: project protocol/evolution loop, Skill Library furnace/sets/market, model and agent binding bug fixes, assistant template app, and Windows packaging.
  - Root-cause notes: `get_active_models` currently builds invalid `pm.pm.id` SQL; Work input folder button is an ambiguous panel toggle; Agent built-in model options all resolve to the same default binding value.
- Completed third refactor slice implementation:
  - Fixed `get_active_models` by replacing the malformed dynamic `pm.pm.id` SQL with an explicit joined select of enabled platform models.
  - Removed the ambiguous Work input folder button and changed Work model values to provider-qualified model routes.
  - Extended `agent_platform_bindings` with `binding_kind` and `builtin_model`, and split Agent binding into Agent default, Agent official/builtin, and OMNIX enabled model options.
  - Added project protocol Tauri commands and schema for status, init preview, confirmed init, events, archive/distillation, protocol actions, and evolution proposals.
  - Added workspace modal protocol preview/confirmation without overwriting existing project files.
  - Added persistent Skill Sets and a `sync_skill_set_to_tools` command; reorganized Skills into library, furnace, sets, market, and sync sub-sections.
  - Replaced the static-only Skill marketplace surface with real `search_skill_market` search plus preview/import-to-local draft behavior.
  - Added the Assistant resource app backed by `agentTemplateApi.getAll()`, with favorites, copy, and one-click handoff into Work input.
  - Verification passed: `npx.cmd tsc --noEmit`, `cargo check`, and `npm.cmd run build`.
  - Full Tauri bundle built the release exe but failed in WiX MSI `light.exe`; NSIS-only bundle passed and produced `src-tauri/target/release/bundle/nsis/omnix-app_0.1.0_x64-setup.exe`.

## 2026-06-14

- Investigated the MSI packaging failure with a systematic WiX/Windows Installer split:
  - `light.exe` normal validation fails at ICE01/ICE02/ICE09 with Windows Installer Service access errors.
  - `light.exe -sval` links a diagnostic MSI successfully, proving the generated WiX object and file binding are not the root problem.
  - Windows Installer service, `WindowsInstaller.Installer` COM, and VBScript/JScript registry entries are present, but WiX ICE validation still fails from this process.
- Added packaging scripts: `tauri:build`, `tauri:build:msi`, `tauri:build:nsis`, `diagnose:msi`, and `repair:msi-env`.
- Added `scripts/diagnose-msi.ps1` for repeatable MSI diagnostics and `scripts/repair-msi-environment.ps1` for elevated Windows Installer/script-engine repair.
- Added a stable WiX `upgradeCode` to `src-tauri/tauri.conf.json`; generated `main.wxs` now contains `1339290b-b4f1-5e65-b4b9-f8b0141ebc54`.
- Verification:
  - `npx.cmd tsc --noEmit` passed.
  - `cargo check` passed with existing unused/dead-code warnings.
  - `npm.cmd run build` passed.
  - `npm.cmd run diagnose:msi -- -RunSuppressValidation` reproduced normal ICE failure and confirmed `-sval` diagnostic linking succeeds.
  - `npm.cmd run tauri:build:msi` and `npm.cmd run tauri:build` still fail at WiX `light.exe` until elevated repair can run.

## 2026-06-17

- Resolved the long-standing MSI packaging blocker with elevated Windows Installer repair.
  - Ran `npm.cmd run repair:msi-env` from an elevated PowerShell (admin session). First attempt aborted because `scripts/repair-msi-environment.ps1` threw on `msiexec.exe /unregister` leaving `$LASTEXITCODE` empty (`$null -ne 0` true). Fixed `Invoke-Native` to treat an unset exit code as success; re-ran and the full repair completed.
  - `npm.cmd run diagnose:msi` now passes end-to-end: Windows Installer service Running, `WindowsInstaller.Installer` COM present, VBScript/JScript x64+x86 registered, WiX `light.exe` normal validation returns exit code 0 (only non-fatal ICE03/ICE40/ICE57/ICE61 warnings remain).
  - `npm.cmd run tauri:build:msi` produced a validated MSI: `src-tauri/target/release/bundle/msi/omnix-app_0.1.0_x64_en-US.msi` (11.8 MB, today 13:44). The MSI ICE validation access-error blocker is cleared.
  - Recorded the `Invoke-Native` empty-exit-code bug in `error-ledger.md`.

## 2026-06-24

- Started the first "real gap" milestone after a repository-wide truthfulness audit.
- Locked the first deliverable to Claude Code and Codex single-Agent work; Skills/distillation, Team, and Knowledge/Quick Assistant remain ordered follow-up milestones.
- Confirmed current baseline: TypeScript and Vite build pass, `cargo check` passes with 126 existing warnings, and the previously failing prompt-guard regression now passes.
- Confirmed concrete gaps: Work model/permission/mode selections do not consistently reach the runtime, assistant output is not persisted, Codex managed install creates a mock CLI, Team uses a simulation panel, Knowledge selection searches the global pool, and marketplace imports can create placeholder skill content.
- Selected structured adapters (Claude stream JSON, Codex app-server JSON), system-first isolated managed installation, per-session model override precedence, in-place SQLite migration, and a package checkpoint immediately after the single-Agent closure.
- Implemented the first real single-Agent runtime milestone:
  - Added a unified Claude Code/Codex runtime model, structured event normalization, permission/work-mode mapping, model precedence and compatibility, official managed-install metadata, and Windows system-CLI shim normalization.
  - Claude Code uses stream JSON; Codex uses the app-server JSON protocol. PTY remains isolated as legacy compatibility and is no longer presented as structured runtime output.
  - Added persistent `agent_sessions` and `runtime_events`, enriched message metadata/order/status, start/send/approve/stop/resume commands, and app-restart history restoration.
  - Added a session-scoped model gateway with Anthropic/OpenAI Responses routes, primary-key plus failover behavior, and per-key status/latency/error recording without logging secrets.
  - Work now loads protocol-compatible models, confirms full access per session, displays structured approvals, stops superseded processes when configuration changes, and loads a real workspace file tree, Git branch, and changes.
  - Memory distillation preview now loads persisted conversation messages instead of hard-coded mock transcripts or placeholder history.
  - Removed known mock conversation seeding, exact-cleaned the two historical mock IDs, removed the unused Team simulation tree/command, and made Team an explicit pending capability surface.
  - Removed unused `model_provider.rs` and `proxy_middleware.rs` abstractions after moving required behavior into the active runtime and gateway modules.
- Verified local real CLI availability without spending a model request: Claude Code `2.1.179`, Codex `0.139.0`, and a live Codex app-server `initialize` response over persistent stdio.
- Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`, `cargo check`, and `cargo test --lib` (`60 passed`, `0 failed`, `4 ignored`). Runtime-focused tests passed `19/19` before the Windows shim regression test was added. Rust warning count fell from 126 to 89 in `cargo check`.
- A fresh Windows package was not produced. The required non-sandbox Tauri build approval and both available local visual-QA paths were rejected by the host usage/security controls before execution. Existing MSI/release-exe timestamps are 2026-06-24 08:47 and the NSIS timestamp is 2026-06-23 21:46, all predating this runtime milestone.

- Completed the remaining real-gap implementation:
  - Added named knowledge bases, scoped multi-base RAG selection for ordinary chat, citation metadata, and activity-key decryption for direct RAG requests. Workspace and Team flows remain unbound by default.
  - Added conservative Quick Assistant auto-capture, application/window blacklist checks, and native file/folder selection for Knowledge imports.
  - Replaced placeholder Skill market imports with real GitHub `SKILL.md` preview/import and provenance hashes. Added model-selected fusion drafts with explicit conflict output and approve/reject application.
  - Marked Claude/Codex skill sync as verified and Gemini/OpenCode/other layouts as experimental instead of implying complete support.
  - Added a unified distillation inbox. Conversation evidence produces pending memory, skill, and protocol candidates; only approved memory/skill candidates write to their stores.
  - Implemented real Team manager planning through the Claude/Codex runtime, mandatory user approval, dependency/concurrency scheduling, Worker approvals, retries, stop, persistence, and final manager validation.
  - Rebuilt the Memory page around the evidence-backed inbox and removed the legacy auto-write command from the public Tauri surface.
  - Fixed the Status Dock click/drag conflict by limiting drag to its grip, and changed the minimum desktop size from 1024x680 to 860x600 with compact icon-only header labels at narrow widths.
- Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`, `cargo check`, and `cargo test --lib` (`65 passed`, `0 failed`, `4 ignored`).
- Native window inspection confirmed the 150% DPI failure mode: 1024 logical pixels can consume the full 1536 physical-pixel display width. The new minimum and compact header address the root cause.
- Closed the release-candidate integration and high-DPI edge cases:
  - Team stop now observes cancelled runtime sessions within 500 ms instead of waiting for the full turn timeout; manual retry requeues blocked downstream assignments. Added a regression test.
  - Added display-aware initial sizing, a 640x520 minimum, compact navigation/sidebar behavior, and narrow-window workspace-panel defaults.
  - Investigated a suspected WebView2 DPI mismatch. The apparent half-width crop came from a DPI-unaware PowerShell capture process receiving virtualized coordinates; in-process Tauri `inner_size` and Win32 `GetClientRect` both reported the full 2560-pixel client width. Kept a defensive native/reported-width correction for genuine mismatches, but removed the temporary frontend zoom permission and diagnostic title code.
  - Added a short-height Work welcome state that hides secondary copy/cards while preserving the title, composer, model, permission, mode, knowledge, search, and send controls.

## 2026-06-25

- Completed final verification after the short-window adjustment: `npx.cmd tsc --noEmit`, `npm.cmd run build`, `cargo check`, and `cargo test --lib` all pass; Rust tests report 70 passed, 0 failed, and 4 ignored.
- `cargo check` reports 90 known legacy warnings, down from the 126-warning baseline.
- `npm.cmd run tauri -- build` passed normal WiX validation and NSIS generation. Fresh artifacts were timestamped 2026-06-25 06:24-06:25.
- Launched the release exe and used a Per-Monitor-V2 native capture process. The full 2586x1655 physical window and the 1280x1040 physical / 640x520 logical minimum window both show complete non-overlapping controls.
- Created `docs/OMNIX-Workbench-0.1.0-发布清单.md` with sizes, SHA256 values, gate results, real capabilities, and explicit boundaries.
- Copied MSI, NSIS, release exe, complete detection plan, user manual, and release manifest to the Desktop `OMNIX Workbench 0.1.0 测试版` folder.
- Began user acceptance (round A). Verified worktree health (`tsc` clean, release exe present, Desktop delivery intact), real CLIs on PATH (Claude Code `2.1.179`, Codex `0.139.0`), and launched the release exe successfully.
- Acceptance found two real bugs; root-caused and fixed both:
  - Codex session start failed with "did not return a thread id within 5 seconds" then "管道正在被关闭 (os error 232)". Reproduced against the real `codex app-server` and inspected the authoritative protocol schema (`generate-json-schema`): method names (`thread/start`/`turn/start`) and the `result.thread.id` response shape were already correct. Real cause: Codex 0.139.0 boots the user's `~/.codex/config.toml` MCP servers synchronously during `thread/start`, so the response arrived after the hard-coded 5s budget (observed ~6s with 6 MCP servers, several failing with os error 267). Raised the budget to 30s (`CODEX_THREAD_START_TIMEOUT`), added eager child-process liveness detection so a dead Codex yields an actionable error instead of a broken-pipe write, moved the readiness wait into `launch_session` so failures surface at start and the first message is instant, and pointed the error text at the MCP config.
  - All Agents shared one chat thread because the Work-page Agent strip only flipped `activeAgent` while keeping the open conversation. Per user decision (Model A), switching Agent now switches to that Agent's own latest conversation via a new `selectAgent` action; each Agent keeps independent history; the work-context sidebar lists only the active Agent's conversations; new conversations bind to the active Agent.
  - Verification passed: `cargo check` (90 known warnings), `cargo test --lib runtime` (22 passed), `npx.cmd tsc --noEmit`, and `npm.cmd run build`.
- Studied three reference projects (cc-switch, AionUi, Cherry Studio) and wrote a phased borrowing roadmap; landed roadmap phase P1 (Codex/Claude default model truly reaches the runtime):
  - Root cause was two-layered: (1) the Work-page model selector defaulted to "Agent 官方默认" which sends `model: null`, so Codex used its own `gpt-5.5/openai`; (2) the session gateway hard-rejected non-Responses providers and did no protocol translation, while Codex 0.139 only emits `wire_api="responses"` and most user providers (DeepSeek/Volcano) only speak Chat Completions.
  - Captured a real Codex Responses request from a live `codex app-server` turn to shape the translator precisely (fixture `src-tauri/src/fixtures/codex_responses_request.json`).
  - Added `responses_bridge.rs`: Responses→Chat request translation and a streaming Chat→Responses SSE translator; wired into `proxy.rs handle_responses_for_session` (native Responses providers still forwarded verbatim; Chat providers translated to `/chat/completions`).
  - `evaluate_model_compatibility` now marks Chat-Completions providers as Gateway-selectable for Codex.
  - Resolution: `runtime_start_session` and `load_runtime_model_options` resolve Agent binding → global `default_model` setting → Agent default, and mark the default option (`is_default`); the Work page now pre-selects it.
  - Validated end-to-end against the real `codex app-server`: Codex accepted the translator's exact Responses SSE output, completed the turn, and surfaced the assistant text. Request translation, streaming text, tool calls, and non-streaming fallback are unit-tested.
  - Verification passed: `cargo test --lib` (76 passed, 4 ignored), `cargo check` (90 known warnings), `npx.cmd tsc --noEmit`, `npm.cmd run build`. Reference repos cloned to gitignored `scratch/`.
- Acceptance UX round after P1 (frontend only):
  - Codex empty model list: added an inline hint in the Work composer when the active Agent has custom models but none are selectable (e.g. only Anthropic providers enabled for Codex), explaining Codex needs an OpenAI-protocol provider. Root cause was config, not code — the user's only enabled provider is Anthropic-typed (Volcano Ark `/api/coding`), which Codex can't use; the gateway translates Codex↔OpenAI-Chat, not Codex↔Anthropic.
  - Split 对话 (Chat) and 工作 (Work) into two distinct top-level surfaces (user choice). A conversation is Chat when unbound (`workspace_path = "direct"`) and Work when bound to a workspace. New `chat` registry entry (default surface), `surface` prop on `ChatTab`, `enterSurface`/surface-aware `selectAgent` in `useConversations`, sidebar lists each surface's own conversations, and Work now requires selecting a workspace before sending (CTA + disabled send).
  - Tab-switch lag: preload lazy tab chunks on idle in `App.tsx` so switching no longer flashes the Suspense loader.
  - First-token wait: added a "正在启动 … 并等待响应" indicator (`startingConversations` in the hook) shown from send until the first runtime event, covering the Codex thread/start MCP-boot delay.
  - Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`.
- Fixed two more acceptance findings on the new Work surface: 工作 no longer auto-defaults a stale workspace (`newConversation` and surface entry reset to `direct`, and 工作 no longer silently reopens a previous workspace; "新工作会话" opens the workspace picker), and the delete-confirm dialog no longer overflows the sidebar (it was `fixed` inside a `backdrop-blur` aside that became its containing block — now portaled to `document.body`). `tsc` + `build` pass.
- Landed roadmap **P2 (统一模型中心)**:
  - Added a global default-model picker (the keystone tying to P1): a ☆ on each enabled model in `PlatformSubTab` sets `default_model` (`platform_id:model_name`) via `settingsApi`, with a header badge showing the current default. The runtime already falls back to it (Agent binding → global default → Agent default) and the Work page pre-selects it.
  - Added ZCF-style provider presets to `PlatformModal`: a quick-preset selector prefills protocol + endpoint (DeepSeek, 火山 OpenAI-兼容, OpenAI, Anthropic, OpenRouter, SiliconFlow, GLM, Kimi, 百炼, Ollama, LM Studio) so users don't mis-configure (the user's earlier Volcano-as-Anthropic mistake). Only the API Key is left to enter.
  - Capability icons (vision/reasoning/coding/tools/…) and health-check visualization already existed in `PlatformSubTab`; P2 completes the Cherry-style model center.
  - Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`. Deferred: surfacing provider presets that call `apply_api_preset` directly (form-prefill chosen instead to avoid reload plumbing).
- Fixed the P2 follow-up acceptance findings:
  - The Work-page model selector did not pre-select the Agent default after switching Agents (e.g. set a ☆ default, switch to Claude Code, still showed "Agent 官方默认"). Root cause: the selector preserved the previous Agent's `selectedModelId`, and the shared `agent_default` option id is valid for every Agent, so it masked the new Agent's `is_default`. Fixed by always selecting the Agent's default on Agent switch.
  - Clarified that the new Models ☆ default (`default_model`) and the existing Settings "默认大模型" (`target_model`) are NOT duplicates — `target_model` drives OMNIX's built-in features (划词翻译/语言检测/知识库, via the general proxy gateway), `default_model` drives the Agent runtime when no per-Agent binding exists (via the session gateway). Renamed Settings → "内置功能默认模型" with a clarifying description, and relabeled the Models ☆ controls as "Agent 默认". Kept both (deleting `target_model` would break translation/QA).
  - Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`.
- Landed roadmap **P4 (Team 协作看板)** v1:
  - Added `src/components/TeamGraph.tsx` — a deterministic layered (topological) DAG of a Team run's Workers, colored by live status (longest-path depth layout, bezier dependency edges, pulsing running nodes), backed entirely by `team_get_run_detail`'s existing worker data (no new backend).
  - Integrated into `TeamTab`: renamed the section to 协作看板, added a per-status count summary and the dependency graph above the worker list; clicking a graph node highlights and scrolls to that Worker card. Live updates ride the existing 1.5s detail poll.
  - Deferred honestly (no fake panel): an Agent-to-Agent mailbox panel — `team_runtime` does not currently populate the `aionui.rs` mailbox, so that is a separate follow-up rather than a placeholder.
  - Verification passed: `npx.cmd tsc --noEmit`, `npm.cmd run build`. The visual board needs a real Team run (manager planning spends a model request / user credentials).
- Landed roadmap **P3 (MCP 一次配置全同步)**:
  - Added `src-tauri/src/commands/mcp_sync.rs` (+ `toml_edit = "0.22"` dependency) — syncs OMNIX-managed MCP servers into the agents' native config: Claude `~/.claude.json` `mcpServers` (serde_json merge) and Codex `~/.codex/config.toml` `[mcp_servers.<name>]` (toml_edit, preserving comments/other tables/model_providers). Commands: `mcp_sync_to_agents`, `mcp_remove_from_agent`, `mcp_get_agent_states`.
  - Safety (these are the user's real config files — a bad MCP entry previously slowed Codex's thread/start): backs up via `crate::backup::backup_file` before writing; merge-only (upsert by name, never bulk-delete); preserves all other config; re-parses rendered output before replacing; atomic temp-file+rename write. Codex stdio-only (remote http/sse servers reported as skipped).
  - Frontend: `mcpSyncApi` wrappers + `McpSubTab` per-server controls — a 同步 button (writes both agents), per-agent ✓/— sync-state chips loaded from `mcp_get_agent_states`, and click-to-撤销.
  - Verification: `cargo test --lib mcp_sync` 3 passed (TOML merge preserves existing config + adds stdio server; Claude stdio vs remote spec; remote skipped for Codex); `npx.cmd tsc --noEmit`, `npm.cmd run build` pass. Live write to a real agent config still needs user testing.
- Made MCP a focused page (user feedback): opening MCP from the title bar previously rendered the full `SettingsTab` (System + Backup tabs visible, looked like Settings). Exported `McpSubTab`, added focused `src/components/tabs/McpTab.tsx` (mirrors ModelsTab), routed `activeTab === "mcp"` to it, and removed the `mcp` sub-tab from the Settings tab bar (Settings now only system + backup). `tsc` + `build` pass.
- Landed roadmap **P5a (助手模板库预设)**:
  - The assistant library was already rich (47 built-in templates via `agent_templates.rs::get_all_templates`, surfaced by `AssistantsTab` with category filter/search/favorites/copy/one-click handoff into Work). The genuine gap vs AionUi/Cherry was document-creation assistants.
  - Added a new 办公 (Office) category with 6 assistants — PPT 制作, Word 文档撰写, Excel 表格/数据, 学术论文写作, 会议纪要, 周报/工作汇报 — the first three reference this project's existing pptx/docx/xlsx skills.
  - Verification: `cargo check` passes (no errors). New templates surface in the Assistants page (53 total).
- Landed roadmap **P5b (Skills 生成)**:
  - The workspace-skill-generation backend already existed but had no UI (`scan_workspace_for_skills` / `generate_skill_from_files` via `skillGeneratorApi`, plus a registered-but-unwrapped `create_skill`). Added a `create` wrapper to `skillLibraryApi` and a new "生成" section to `SkillHub`: pick workspace → scan files → select + name → generate a SKILL.md draft → review → save as a local skill (or copy).
  - Verification: `npx.cmd tsc --noEmit`, `npm.cmd run build` pass.
- **P5c (远程/定时)**: assessed as already implemented — not building filler. Remote cross-device access (`get_remote_access_info` → Dashboard "远程跨设备调试") and unattended cron (`CronTab` with create/edit/toggle/delete/history + an existing "立即运行" manual trigger via `trigger_cron_task`) both already exist and work. The three-tier skill source idea (builtin/custom/extension) was not forced because skills only carry a `source_type` flag, not a meaningful three-tier taxonomy; the generation feature delivers the concrete P5b value instead.
- Borrowing roadmap P1–P5 complete (P5c was already present).
- Released **0.2.0**:
  - Bumped version to 0.2.0 in `package.json`, `src-tauri/Cargo.toml`, `src-tauri/tauri.conf.json`; wrote the 0.2.0 CHANGELOG entry.
  - Built fresh artifacts via `npm run tauri build`: standalone `omnix-workbench.exe`, `OMNIX Workbench_0.2.0_x64-setup.exe` (NSIS), and `OMNIX Workbench_0.2.0_x64_en-US.msi`. Copied the standalone exe + NSIS + MSI + CHANGELOG to the Desktop `OMNIX Workbench 0.2.0` folder for user testing.
    - Standalone exe SHA256: `8E45DAB3A72D996B0CAD25A1020D0FBDB640D1ACBADC14DA65351D275E7FD769`.
    - NSIS setup SHA256: `DDF0ECD2048E5BDE1CDBE1DB9F18510BFAC1B3A69DDA7A3920380D11D1371E12`.
  - Git consolidation: committed all P1–P5 work, fast-forwarded `master` to the release commit, pushed to `origin/master`, and deleted the now-merged `feat/msi-packaging-and-refactor-slice` branch (local + remote). `master` is the only branch and GitHub's default.

## 2026-06-26 (cont.) — desktop-app roadmap

- Researched the 2026 Codex / Claude Code desktop apps (WebSearch, post-cutoff) and wrote a phased roadmap (R1–R6) in the approved plan: R1 checkpoints+diff, R2 panel workspace+file preview, R3 worktree parallel+background, R4 context-compaction+token UI+hooks, R5 划词助手 deepen + notes + Cherry suite, R6 Cowork/computer-use. Classified each requested feature as exists-deepen vs new.
- Landed **R1 (检查点/回滚 + 逐文件 diff 审查)**:
  - New `src-tauri/src/commands/checkpoints.rs`: snapshots the whole working tree onto a **shadow ref** `refs/omnix/checkpoints/<id>` via a temp index (`add -A` → `write-tree` → `commit-tree` → `update-ref`), capturing tracked + untracked (non-ignored) content without touching the user's index, working tree, or `git log`. Commands: `create_checkpoint`, `list_checkpoints`, `get_workspace_diff` (temp-index `diff --cached <base>` so new files show as additions), `restore_checkpoint` (read-tree + checkout-index + remove files not in the checkpoint; auto-creates a pre-restore checkpoint), `revert_file` (restore one file, or delete if new). Lazy `checkpoints` table. Non-Git workspaces return a skipped checkpoint (no-op), not an error.
  - Auto-checkpoint wired into `useConversations.sendMessage` before a Direct-mode + real-workspace turn (skipped/failures never block the turn).
  - New `src/components/WorkspaceCheckpoints.tsx` mounted in the ChatTab workspace panel: per-file diff (status badge, +/- counts, expandable colored unified diff, single-file 还原/reject) + checkpoint timeline (回退到此/restore with confirm).
  - Verification: `cargo test --lib` 80 passed (incl. checkpoint create→diff(M+A)→restore round-trip against real git), `cargo check`, `tsc --noEmit` pass. Live test needs a real Direct-mode workspace turn.
- Landed **R2 (文件预览面板)** — the headline of the panel-workspace phase:
  - Backend `read_workspace_file` in `commands/workspace.rs`: path-traversal-guarded read that classifies by extension/content — image (base64 data URL), pdf (base64 data URL), markdown/text/code (UTF-8 up to 512 KB, with truncation flag and a highlight-language hint), known-binary/Office → binary kind. Uses the existing `base64` crate.
  - Frontend `src/components/FilePreviewPanel.tsx`: a wide right drawer rendering by kind — image inline, PDF in an iframe, Markdown via react-markdown, code/text as monospaced `<pre>`, binary/Office → "用系统应用打开". Opened by clicking a file in the workspace tree or a changed-file entry (directories still open in the system); closeable; "open externally" fallback.
  - Verification: `cargo test --lib workspace` (read text + block traversal) passed, `tsc --noEmit` pass.
  - Scope note (honest): R2 delivers the in-app file preview. The fuller "arrangeable multi-pane layout + embedded terminal pane" (drag/rearrange) remains a follow-up; the workspace panel now has checkpoints/diff (R1) + file preview, which covers the highest-value Claude-Code-desktop "Code tab" behaviors. Office (docx/xlsx/pptx) in-app rendering is intentionally deferred (opens with the system app) to avoid heavy viewer dependencies.
- Landed **R3 (worktree 并行会话)** — the headline of the parallel-sessions phase:
  - New `commands/worktrees.rs`: per-session Git worktree isolation. `create_worktree` makes a fresh `omnix/<branch>` at HEAD checked out under a sibling `.omnix-worktrees/<repo>__<branch>` dir (never inside the repo tree) and records id↔session↔branch in a lazy `worktrees` table; `list_worktrees` reconciles `git worktree list --porcelain` ground truth with the DB metadata and enriches each entry with `dirty` (porcelain status) + `ahead` (commits not in main HEAD); `remove_worktree` runs `worktree remove [--force]` + `prune` + optional branch delete; `merge_worktree` does `merge --no-ff` into the main tree's current branch and, on conflict, **aborts** so the main tree is left clean and surfaces the conflict honestly (never auto-resolves). Windows fix: strip the `\\?\` verbatim prefix from canonicalized paths before handing them to `git worktree add` (it cannot create leading dirs under that form).
  - Frontend `src/components/WorktreePanel.tsx`: lists the repo's parallel worktrees with per-item branch, 未提交/↑ahead/已失效 badges, and 打开 (system file manager) / 合并 (disabled when ahead==0) / 移除 (force when dirty, confirm) actions, plus 新建 to spin up an isolated worktree for the current session. Mounted in the ChatTab workspace panel under checkpoints.
  - Verification: `cargo test --lib worktrees` (create→list→commit-in-worktree→ahead==1→clean merge→main gets the file→remove cleans up) passed; `tsc --noEmit` pass. App relaunched (dev), proxy on 127.0.0.1:1421.
  - Scope note (honest): R3 delivers the **worktree parallel-session** capability (create/list/merge/remove isolated trees) — the concrete, highest-value piece. The other two R3 ideas from the plan — an in-session **background-task / sub-agent panel** and **process-overlap (pipelining)** — are deferred: both require turn-engine changes in `runtime_manager.rs` (running a second agent turn concurrently and reconciling its events into one session) that carry real correctness risk, so they are intentionally not faked here. The worktree layer is the prerequisite that makes safe parallel agent work possible; wiring an auto-worktree-per-background-task on top of it is the natural R3 follow-up.
- Landed **R4 (Token 活动消耗与费用面板)** — surfacing the token-economy data that was collected but never shown in the UI:
  - Architecture finding (drove the honest scope): OMNIX's agent runtime **delegates** to the external CLIs — `runtime_manager::send_message_with_display` forwards a prompt to the Codex thread / Claude Code session, which own their conversation history and context window and self-compact. So "auto context-compaction of the history OMNIX sends to the model" does **not** apply to agent-CLI sessions (they handle it); it only applies to OMNIX's own gateway/direct-chat path, where `token_economy::compress_tool_result` / `estimate_tokens` are already exposed as commands. Recorded this rather than building a fake "compaction" gauge over context OMNIX doesn't own.
  - Backend (`commands/lifecycle.rs`): the `request_logs` table + `get_usage_stats` already aggregated tokens but **no UI surfaced it** (only `tauri-api.ts` referenced it). Added estimated **cost** via the existing `circuit_breaker::estimate_cost` pricing table — `UsageStats` now carries `total_cost_usd` + `cost_today_usd`, `ModelUsage` carries per-model `cost_usd` (summed per-model so each uses its own rate, via a `sum_cost` helper). New command `get_usage_timeseries(days)` returns gap-free-able daily `{date, requests, tokens, cost_usd}` buckets (per-day per-model accumulation so cost is rate-correct), registered in `lib.rs`.
  - Frontend `src/components/TokenActivityPanel.tsx`: focused panel with today/total **token + 费用** cards, requests + avg-latency/error cards, a **daily activity bar chart** (7/14/30-day toggle, Tokens↔费用 metric toggle, gap-filled axis, per-bar tooltip) and a per-model consumption list with cost. Mounted in `DashboardTab`, **replacing** the old "常用模型 TOP 5" card (removed its now-redundant local state/imports). Honest footnote in the panel: cost is estimated; stats are OMNIX-gateway requests only — CLI-direct agent sessions bill themselves.
  - Verification: `cargo check --lib` clean, `tsc --noEmit` pass. App relaunched (dev), proxy on 127.0.0.1:1421.
  - Scope note (honest): R4 delivers the **token/cost activity surface** (the user's explicit "token 活动消耗" emphasis) plus the recorded compaction finding. The other R4 plan items — **user-state hooks** and **remote-control enhancement** — landed in the R4-completion pass below.
- Completed the rest of **R4 (用户态 hooks 引擎 + 远程控制增强)**:
  - **Hooks** — new `commands/hooks.rs`: event→action rules (Claude Code hooks inspired). A `hooks` table (id, name, event, matcher, action_type, action_payload, enabled, fire_count, last_fired_at) + a bounded `hook_runs` log (capped at 500). `evaluate_hooks(db, app, envelope)` runs inside the existing runtime-event consumer loop in `lib.rs` (before the `agent-session-event` emit): it filters to a `HOOKABLE_KINDS` set (session_started / tool_started / tool_completed / approval_requested / turn_completed / error — noisy delta/raw_log/message kinds excluded), loads matching enabled hooks into a `Vec` and **drops the DB connection before running any action or spawning a process** (CLAUDE.md 坑点 2 — no `MutexGuard` across the loop await/spawn). Actions: `notify` (emits the existing `omnix-notification` event), `command` (fire-and-forget `cmd /C` / `sh -c`, with env `OMNIX_SESSION_ID` / `OMNIX_EVENT` / `OMNIX_EVENT_TEXT`), `log`. Optional case-insensitive substring matcher against the event text. Commands `list/save/toggle/delete/test_hook` + `get/clear_hook_runs`, registered in `lib.rs`. Frontend `src/components/HooksPanel.tsx` (self-contained: editor with event/action selects, matcher, payload, enable switch, per-hook 测试/编辑/删除, collapsible run log) mounted in the automation hub `CronTab`. The command-action UI carries an explicit "runs a shell command on your machine — only configure commands you trust" warning.
  - **Remote enhancement** — added `POST /api/remote/send` to the proxy (token-gated like the rest of the remote API): delivers a free-text instruction to the active session via the same `agent_manager.send_stdin` channel the approval flow uses, so the phone can now *drive* a session, not just approve. `remote_dashboard.html` gained a free-text "发送" box wired to it. (Honest: this rides the existing stdin channel; it does not add a new turn-orchestration path.)
  - Verification: `cargo test --lib hooks` passed (matcher + log/unknown-action behavior), full `cargo test --lib` build clean (proxy remote-send compiled), `tsc --noEmit` pass. App relaunched (dev), proxy on 127.0.0.1:1421.
  - This closes R4. Deep auto-context-compaction into the agent runtime remains intentionally **not** built (agent CLIs own their context — recorded above); the OMNIX-gateway compaction primitives (`compress_tool_result` / `estimate_tokens`) are already exposed for the direct-chat path.
- Post-R4 follow-up (user feedback): the Hooks UI was initially mounted at the bottom of the experimental `CronTab` (Labs launcher) and the user couldn't find it. **Promoted Hooks to its own focused page** (`src/components/tabs/HooksTab.tsx`), mirroring how MCP got its own page: new `hooks` entry in `appRegistry.tsx` (group `resource`, launcher placement, Webhook icon), lazy-loaded + preloaded + rendered in `App.tsx` (`activeTab === "hooks"`), and removed the duplicate `<HooksPanel/>` mount from `CronTab`. `tsc --noEmit` pass; vite HMR live. Also recorded the deferred R-series follow-ups in `.omx/development/current.md` (R2: arrangeable multi-pane + embedded terminal, Office in-app rendering deferred; R3: in-session background-task/sub-agent panel + pipelining; R4 open question: whether/how to build a context-window meter).
- **R4 (a) context-window meter — built (accurate, scoped to OMNIX-owned transcript).** Finding that shaped it: the main chat (even the `chat` surface) goes through `runtimeApi.startSession` → the CLI runtime, so OMNIX never owns a *growing model context*; the `messages` table is OMNIX's stored transcript (what it replays on resume). So the honest accurate thing to meter is that transcript. Improved `commands/odysseus.rs::get_context_budget` to load per-message content and use a shared **CJK-aware** `estimate_text_tokens` (ASCII ~4 chars/tok, CJK ~2) instead of `SUM(LENGTH)/4`, and added `message_count`. Frontend `src/components/ContextMeter.tsx`: a slim used/window gauge (colored ok/warning/critical) in the chat composer, with a 压缩 action wired to the existing `compact_conversation_context` (summarize old → keep last 20) when the transcript grows large; `onReloadMessages`/`onSelectConversation` props added to `ChatTab` so compaction refreshes the view. Model window via a new `contextWindowFor()` table in `constants.ts` (claude 200K, gpt-4.1/gemini 1M, gpt-5 256K, default 128K). Tooltip is explicit that it measures the OMNIX transcript, not the CLI's live internal context.
- **R3 follow-up — in-session background tasks / sub-agents — built (session-level parallelism, no pipelining).** Honest design: a sub-agent is an **independent child agent session running in its own Git worktree** (R3), concurrent with the parent (RuntimeManager already holds many sessions) — real process overlap WITHOUT the risky intra-session turn pipelining. New `commands/subagents.rs` is the persistence layer only (a `subagents` table linking parent conversation → child session + worktree + status; `create/list/update_subagent_status/delete`). The frontend `src/components/SubAgentPanel.tsx` orchestrates with existing APIs: 新建 → `worktreeApi.create` (isolated tree) → `conversationApi.create` (child conv bound to the worktree) → `runtimeApi.startSession` (concurrent child session, agent_default model, parent's permission, direct mode) → `runtimeApi.sendMessage` (the task). Lists sub-agents with live status (reconciled from `runtimeApi.getSession`), and 打开 (switch main view to the child conv to watch/approve/interact) / 目录 / 合并 (`worktreeApi.merge`) / 停止 / 移除 (stop + remove worktree). Mounted in the ChatTab workspace panel under WorktreePanel (Git workspace only). Honest limitation noted in-code: a background sub-agent that hits an approval prompt pauses until the user opens it and responds (no silent auto-approve).
- Verification (both): `cargo test --lib odysseus` (compiles; no unit tests there) + full `cargo check --lib` clean (subagents + context-budget), `tsc --noEmit` pass. App relaunched (dev), proxy on 127.0.0.1:1421. These close R4 (a) and the R3 sub-agent follow-up; the R3 **pipelining** (concurrent turns reconciled into one session) remains intentionally unbuilt.
- **R5 (划词助手深挖 + 笔记) — built.**
  - **Custom Quick Actions (划词助手深挖, the user's 重点)** — new `commands/quick_actions.rs` (`quick_actions` table: label/emoji/prompt_template/enabled/order; list/save/delete). The popup `src/QuickAssistant.tsx` previously had 6 hardcoded actions (translate/explain/summarize/refine/search/copy); now it also loads enabled custom actions and renders them in the capture bar. Refactored the streaming logic into a shared `streamPrompt(fullQuery)` runner used by both built-in explain/summarize and custom actions; a custom action's `prompt_template` substitutes `{{text}}` with the selection (appended if absent). Config UI: new self-contained `src/components/QuickActionsEditor.tsx` (emoji + label + template + enable, CRUD) mounted in `QuickAssistantTab` under 翻译与动作.
  - **Notes (笔记, brand new)** — new `commands/notes.rs` (`notes` table: title/content/tags/source; list-with-search/save/delete) + focused page `src/components/tabs/NotesTab.tsx` (searchable list + Markdown editor with live preview), registered as its own launcher app in `appRegistry.tsx` (resource group, StickyNote icon) and wired into `App.tsx`. Integration: the Quick Assistant result footer gained a **存为笔记** button (`handleSaveNote`) that saves the selection + result as a Markdown note with `source = 划词助手`.
  - Verification: `cargo check --lib` clean (quick_actions + notes), `tsc --noEmit` pass. App relaunched (dev), proxy on 127.0.0.1:1421.
  - Scope note (honest): R5 delivers the 划词助手 deepen (the 重点) + Notes (the brand-new piece) with end-to-end integration (QA → note). The remaining R5 plan items — deeper 知识库/助手库/翻译/**多模型同对话**(`CompareHub` → one-question-many-models side-by-side) — are existing features to *deepen* and are deferred to a follow-up slice rather than half-touched this turn.
- **划词助手常驻自动弹出修复 (user feedback).** Complaint: selecting text didn't pop anything up. Two root causes: (1) the auto-capture monitor (`selection.rs::start_auto_capture_monitor`, UIA poll every 500ms) emitted `selection-auto-captured` to the QA webview but **nothing ever showed the OS window** — and the `quick-assistant` window is created `.visible(false)`, so the event reached a hidden window and the user saw nothing. (2) Auto-capture defaulted OFF. Fixes: added `get_cursor_position()` (Win32 `GetCursorPos`) + `show_popup_near_cursor()` and call it in the monitor right before emitting, so on a fresh selection the popup is **shown + positioned next to the cursor** (Cherry Studio style — no copy, no hotkey). Added a self-skip guard so the monitor never captures OMNIX's own windows (the popup gains focus when shown, which would otherwise re-capture its own result text). Made auto-capture **default ON** in `useSelection.ts` (unset → enabled; only an explicit "false" disables; toggle still in 划词助手 settings). Verified `cargo check --lib` + `tsc` clean; app relaunched. Dismiss stays as ESC / ✕ (did not add blur-to-hide to avoid hiding in-progress results).
- **R5 remaining — 多模型同对话 (CompareHub multi-turn).** CompareHub already did one-question-many-answers side-by-side (API parallel SSE + native web views + a fusion summary), but the API mode was **single-shot** — each submit replaced the answers. Deepened it into a real **multi-turn conversation**: replaced the single `apiResults` map with `threads: { [accId]: TurnMsg[] }` (per-model conversation). Each submit now builds each model's FULL prior history (excluding in-flight/errored turns), appends the new user turn, fans it out to every selected model concurrently, and streams the reply into a new assistant message in that model's column (via a `patchLastAssistant` helper). Columns now render the running thread (user bubbles + assistant answers) instead of just the latest answer; added a "💬 继续追问" submit label and a "🆕 新对话" reset. The fusion furnace now reads each model's latest assistant answer (`latestAnswers()`); web mode + fusion otherwise unchanged. `tsc --noEmit` clean; frontend-only → vite HMR live.
  - Scope note (honest): this delivers the plan's named "多模型同对话" deepen. The other R5 deepen items — 知识库 (multi-source/citations), 助手库 (favorite/share), 翻译 — are existing features left as-is for a later polish pass; not faked.
- **R5 polish — 知识库 / 助手库 / 翻译 (the remaining three).**
  - **知识库 (citations):** found it already does multi-source attribution — hybrid-search results show `knowledge_base_name / document_title` + BM25/Vec/RRF scores, and RAG chat shows sources. Did NOT fake a change there; added one genuine small touch: a **"引用"** button on each search result (`KnowledgeHub.tsx`) that copies the chunk as a Markdown blockquote with its source attribution, for pasting into notes/docs.
  - **翻译 (history surfaced):** `get_translation_history` was recorded by the backend but **no UI showed it**. New `src/components/TranslationHistoryPanel.tsx` (self-contained, uses `useTranslation`): lists past translations with source/target language badges + time, copy译文 / delete / clear. Mounted in `QuickAssistantTab` under the custom-actions editor.
  - **助手库 (custom + share):** built-in templates were read-only with localStorage favorites; no custom assistants, no sharing. New `commands/custom_assistants.rs` (`custom_assistants` table: slug/name/description/category/instructions; list/save/delete). `AssistantsTab` now merges custom assistants (mapped onto the `AgentTemplate` shape, shown first with a 自定义 badge) with built-ins, and adds: **新建助手** (inline form), **导入** (pick a JSON → saved as custom — the share-in path), **分享/导出** (download any assistant as JSON), and **删除** for custom ones. `customAssistantApi` added to `tauri-api.ts`.
  - Verification: `cargo check --lib` clean (custom_assistants), `tsc --noEmit` pass. App relaunched (dev), proxy on 127.0.0.1:1421. **This closes R5 in full.**
- **Three user-requested features: 翻译区 / Agent→笔记 / 知识库迁移.**
  - **Dedicated translation page** — new `src/components/tabs/TranslateTab.tsx` (Google/有道 style): two panes (source ↔ target) over `translationApi.translate` (explicit source/target, auto-detect option), language pickers + swap, model selector (persisted to `translate_model`), copy/clear, Ctrl+Enter, and the existing `TranslationHistoryPanel` below. Registered as its own launcher app (`translate`, Languages icon) in `appRegistry.tsx` + wired in `App.tsx`.
  - **Agent → notebook** — added a "存为笔记" action on assistant messages in `ChatTab.tsx` (`saveMessageAsNote`): saves the message (e.g. a proposed plan / deferred item / suggestion) to Notes with title `[<agent>] <first line>`, tag `agent`, source `<agent> · <workspace>`, so the user can review for missed work in the 笔记 page. (Calling KB content during dev already existed.)
  - **Knowledge-base portability** — new `commands/kb_transfer.rs`: `kb_export_base` serializes a whole base (documents + chunks + embeddings, base64) to self-describing JSON tagged with format/version + the embedding model(s); `kb_import_base` recreates it into a new base (new ids; the `kb_chunks`→`kb_chunks_fts` trigger rebuilds BM25 automatically; embeddings restored verbatim with their model+dimensions). UI: 导出/导入 buttons next to the base selector in `KnowledgeHub.tsx` (download `.omnixkb.json` / pick a file). Answer to the user's question is encoded in the import toast + module docs: switching the **chat** model is harmless; switching the **embedding** model means keyword (BM25) search still works but vector search needs re-embedding (embeddings are model-specific, tagged by model/dimensions). Transfer between OMNIX installs works via the file; other software can read the documented JSON.
  - Verification: `cargo check --lib` clean (kb_transfer), `tsc --noEmit` pass. App relaunched (dev), proxy on 127.0.0.1:1421.
- **User feedback fixes: 翻译模型一致性 / 划词非侵入 / 笔记落盘.**
  - **翻译页 (#1):** removed the per-page model picker entirely — `TranslateTab.tsx` no longer passes `chatModel`, so `translate_text` falls back to its built-in default (`target_model` = 设置→内置功能默认模型). This eliminates the two-places-out-of-sync error the user hit and the "no result" caused by an unroutable picked model. Header now just notes "使用设置→内置功能默认模型".
  - **划词助手非侵入 (#3, Cherry-style):** the popup used to `set_focus()` (stealing focus) and never auto-dismissed, forcing the user to close it before continuing. Now `show_popup_near_cursor` shows the window via Win32 `ShowWindow(SW_SHOWNOACTIVATE)` (rebuilding HWND from the raw handle, version-robust) so the foreground app keeps focus — the user can keep selecting / Ctrl+C / work without interruption. Added `hide_popup` + auto-dismiss in the monitor: after the user deselects/clicks away (two empty UIA polls ≈1s) the popup hides itself; while the user is actually interacting with it, the existing OMNIX-self-skip guard prevents auto-hide. The popup only takes focus when clicked.
  - **笔记落盘 (#2):** notes were SQLite-only. `commands/notes.rs` now mirrors every note to `~/.omnix/notes/<id>.md` on save and removes it on delete (real Markdown files on disk — portable, and a file-based agent can read them). New `get_notes_dir` command + a "打开文件夹" button in `NotesTab.tsx`. (Honest: agent→notes manual save exists; full autonomous agent read/write of notes would need a notes tool/MCP — the on-disk md files are the enabler for "point the agent at the folder".)
  - Verification: `cargo check --lib` clean, `tsc --noEmit` pass. App relaunched (dev), proxy on 127.0.0.1:1421. (Per user: no commit/package yet — batch with R6 + remote.)
- **Three more user-feedback fixes: 翻译空白 / 划词弹窗主题 / 划词监听关不掉.**
  - **翻译空白 (#1):** removing the model picker made TranslateTab pass no model → `translate_text` fell back to `target_model`, and when that setting is unset it used an unconfigured `"deepseek-chat"` → API error → blank result (and so no new history). Fix: TranslateTab now silently resolves the model the same way the Quick Assistant does — `target_model` (内置功能默认模型) else the first available model from `modelApi.getAvailableNames()` — and passes it. No picker shown, no two-places-out-of-sync. Once translations succeed, history populates.
  - **划词弹窗主题 (#2):** the Quick Assistant is a separate webview window; `App.tsx` early-returns the `isQuickAssistant` branch BEFORE `useTheme` runs, so the popup never applied the saved theme and defaulted to dark (black) while the main app was light. Fix: `QuickAssistant.tsx` now reads `theme_mode` and applies `useTheme(themeMode)` itself, re-reading on mount and on each `selection-auto-captured` event so it tracks the main app's theme.
  - **划词监听关不掉 (#3):** the SettingsTab "自动捕获" switch was mis-wired — bound to `selectionShowOnCapture` and only calling `toggleAutoCapture(v)` WITHOUT persisting `selection_assistant_auto_capture`. Combined with the new default-ON (`unset → on`), disabling never stuck across reload (and the switch didn't reflect real state). Fix: added `selectionAutoCaptureEnabled` + `onSetSelectionAutoCaptureEnabled` props (wired through `App.tsx` to `saveSelectionSettings({ autoCaptureEnabled })`, which persists "false" + aborts the monitor + updates state); the switch now binds to the real auto-capture state. Backend `toggle_selection_auto_capture` also hides the popup window when disabling, so a non-focused popup can't be left stuck on screen.
  - Verification: `cargo check --lib` clean, `tsc --noEmit` pass. App relaunched (dev), proxy on 127.0.0.1:1421.
- **User feedback fixes: opener 权限 / 翻译诊断 / 划词关闭 / 笔记 MCP 工具.**
  - **#1 opener:** "无法打开：opener.open_path not allowed" — `capabilities/default.json` only had `opener:default` (which lacks `open_path`). Added `opener:allow-open-path` + `opener:allow-open-url`. Fixes the notes folder button AND every other `openPath` ("打开" in file preview / worktree / sub-agent / translate).
  - **#3 翻译彻底空白:** root cause — `translate_text` posts to the gateway without an agent header; the gateway resolved `target_model` (or unconfigured `"deepseek-chat"`) and either errored or returned an empty body, which the command turned into silent blank text. Fixes: (a) model resolution is now explicit→`target_model`→**first enabled platform model with an API key** (`first_enabled_chat_model`, `platform_id:model_name`), erroring clearly if nothing is configured; (b) the response path reads the body, surfaces non-2xx and parse failures, and **errors on empty translated text** with the raw response snippet — so failures are visible (toast/error box) instead of blank. TranslateTab already resolves the same way client-side.
  - **#3 划词关不掉 / X 无反应:** the no-activate (SW_SHOWNOACTIVATE) popup never has focus, so the activating click on the in-app ✕ gets eaten by WebView2 (first-click swallow). Added blur-to-hide in `QuickAssistant.tsx` (Tauri `onFocusChanged` → hide when focus leaves, guarded by an `isStreamingRef` so an in-flight answer isn't lost) — clicking back to your work now dismisses the popup reliably (Cherry-style), and the ✕ works once the window is focused.
  - **#2 笔记 MCP 工具:** added a "接入 Agent" (Plug) button in `NotesTab` — registers a filesystem MCP server (`omnix-notes` → `npx -y @modelcontextprotocol/server-filesystem ~/.omnix/notes`) via the existing `mcpApi.save` + `mcpSyncApi.syncToAgents(["Claude Code","Codex"], ["omnix-notes"])`, so an agent can list/read/write the user's notes as files. (Honest: needs Node/npx on the machine — which agent CLIs already require.)
  - Verification: `cargo check --lib` clean, `tsc --noEmit` pass, full rebuild for the capability change. App relaunched (dev), proxy on 127.0.0.1:1421. (Per user: still no commit/package.)
- **Root-caused the popup regressions: the no-activate window ate first clicks.** Verified the gateway translates fine (`deepseek-v4-pro` → "Hello"/"test", 200 OK) — the backend was never broken. The real bug: showing the Quick Assistant popup with `SW_SHOWNOACTIVATE` (added to stop focus-stealing) left it unfocused, so WebView2 ate the FIRST click on any button (MA_ACTIVATEANDEAT) — clicking 翻译/总结 did nothing → blank result; clicking ✕ did nothing → "关不掉". Fixes:
  - `selection.rs`: reverted `show_popup_near_cursor` to a normal focused `show()` + `set_focus()` so buttons are clickable on the first click.
  - `QuickAssistant.tsx`: to stay non-intrusive without the no-activate hack, it now auto-dismisses — kept blur-to-hide (click back to your work → popup hides), added an 8s idle auto-dismiss for an un-acted action bar (`scheduleDismiss`/`cancelDismiss`, cancelled once the user runs an action), and the ✕ now calls `hideWindow()` (closes the window, was only hiding the bar). Added `data-tauri-drag-region` to the captured-text header row so the frameless popup is **movable** ("· 可拖动").
  - Net: 翻译/总结/✕ all work on first click; popup is movable; it dismisses when you click away or after 8s idle; theme already follows the app (prior fix).
- **#4 notes folder open:** the `opener:allow-open-path` ACL grant wasn't reliably taking effect. Added a native backend command `open_notes_folder` (explorer/open/xdg-open) used by NotesTab's folder button — bypasses the opener plugin's JS permission entirely, so it always works.
- Verification: `cargo check --lib` clean, `tsc --noEmit` pass, full rebuild; app confirmed **stable** (port 1421 still listening 12s after launch — earlier instances had been dying between my restarts, which made the whole app look blank/frozen to the user) and gateway translation verified (`测试` → `test`).
- **The REAL translation bug (field-name contract mismatch) — finally root-caused via the DB.** The user's screenshot showed a history row that existed but rendered blank; querying `translation_history` directly proved the translation had SUCCEEDED and was stored (en source + correct zh target). So the backend was never broken — the frontend was reading the wrong field names on two return contracts:
  1. **Result blank:** `translate_text` returned `CaptureResult { text, source, window_title, … }` (snake/neutral), but the frontend `TranslateResponse` reads `translatedText` / `detectedLang` / `targetLang` → all `undefined` → blank. (This also explains why the Quick Assistant translation was always blank.)
  2. **History blank:** `db.get_translation_history` mapped `translation_history` rows into the unrelated `SelectionHistoryEntry { captured_text, source, window_title, … }`; the frontend `TranslateHistoryEntry` reads `source_text` / `target_text` / `source_lang` / `target_lang` → `undefined` → only `created_at` matched (which is why the timestamp showed but text didn't).
  Fix: added two dedicated structs in `selection.rs` — `TranslateResult` (`#[serde(rename_all = "camelCase")]` → `translatedText/detectedLang/targetLang`) and `TranslateHistoryEntry` (snake_case → `source_text/target_text/source_lang/target_lang/created_at`); `translate_text` now returns `TranslateResult`, and `db.get_translation_history` + the command return `TranslateHistoryEntry` with a correct 1:1 column mapping. Both now exactly match the existing TS types. The earlier translation "fixes" (model resolution, error surfacing) were treating the wrong layer — kept here as they're still correct hardening, but THIS contract fix is what makes the result + history actually render.
  - Verification: `cargo check --lib` clean; app rebuilt and confirmed stable. The user's pre-existing history row will now render correctly (its data was always valid in the DB).
- **Navigation simplified to two tiers (user request): removed 隐藏.** Now only 固定(标题栏) / 收纳(宫格). `normalizeNavigationLayout` folds any previously-hidden apps into the launcher and always returns `hidden: []`; `AppHeader` dropped the 隐藏 action button (pinned section → only 收纳到宫格, launcher section → only 固定) and the entire 已隐藏 section + its help-text clause and the unused `EyeOff` import. `hidden` is kept as an always-empty array for type/back-compat.
- **R6 (Cowork / computer-use) — foundation landed.** New `commands/cowork.rs`: a per-session authorization gate (`AtomicBool`, defaults OFF, never persisted) with `cowork_authorize`/`cowork_is_authorized`; every action refuses unless authorized. `cowork_screenshot` captures the primary monitor via `xcap` (DPI/physical pixels) and encodes PNG from raw RGBA with the `png` crate → base64 data URL + dimensions. Input injection via Win32 `SendInput` (mirroring selection.rs): `cowork_move` (SetCursorPos), `cowork_click` (left/right down+up), `cowork_type` (Unicode `KEYEVENTF_UNICODE` per UTF-16 unit), `cowork_key` (named keys → VKs). Added `xcap` + `png` deps. Frontend `src/components/tabs/CoworkTab.tsx`: a strongly-worded authorization toggle + kill switch ("收回授权"), a live screenshot (manual + 1.5s auto-refresh) where **clicking on the screenshot clicks the corresponding physical screen point** (image→screen coordinate mapping), left/right/move modes, a text-type box, and named-key buttons. Registered as a `labs`/launcher experimental app (`cowork`, MonitorPlay icon) in `appRegistry.tsx` + `App.tsx`.
  - Verification: `cargo check --lib` clean (Win32 input + xcap/png compiled first try), `tsc --noEmit` pass; app rebuilt and confirmed stable (port 1421 still listening 8s after launch).
  - Scope note (honest): this is the computer-use **foundation + manual testbed** — screenshot, input, authorization, kill switch, and a click-to-control screen. The **autonomous** part (an agent calling these tools in a loop to operate the computer) is the next step and needs these exposed to the agent as a tool/MCP. **Remote-control enhancement** (drive the host from the phone, incl. a remote screenshot view built on `cowork_screenshot`) is the natural follow-up that ties R6 + remote together; not in this slice.
- **远程：手机端查看+控制 OMNIX 对话（AionUi 风格）— Part A + B 落地。** 按用户澄清：**不做远程操作整台电脑**（桌面本地 Cowork 保留），改做"电脑跑 OMNIX、手机浏览器看正在进行的 Agent 对话并输入文字继续/新建会话"。
  - **后端 (`proxy.rs` + `lib.rs` + `commands/`)**：`ProxyState` 注入 `runtime_manager`（之前只有旧 `agent_manager`，够不到 Codex/Claude 运行时会话）；`start()` 增参，`lib.rs` 传入。新增设置 `remote_access_enabled` → 为真时把代理绑到 `0.0.0.0`（默认仍 `127.0.0.1`，仅显式开启才暴露 LAN）。新增 6 个 token 鉴权端点：`GET /api/remote/conversations`（读 `conversations` + 子查询 `agent_sessions` 状态判断 running）、`GET /api/remote/messages?conversation_id`（读 `messages` 表，按 timestamp+rowid 排序）、`POST /api/remote/chat`（发到该会话的最新 runtime session：`send_message_with_display`，失败则 `resume_session` 再发 —— 照搬桌面流程；注意 `send_message` 是 `#[cfg(test)]`，生产用 `send_message_with_display`）、`GET /api/remote/agents`（Claude Code/Codex + 是否已安装）、`GET /api/remote/workspaces`（最近工作区）、`POST /api/remote/new`（建会话行 + 调新加的 `commands/runtime.rs::remote_start_session` 复用配置解析启动）。新增 `set_remote_access` 命令：持久化设置 + **停旧代理 → sleep 800ms → 重新 start**（不跨 await 持 std 锁）。`remote_token` 已在 DB 初始化时自动生成。
  - **前端**：`remote_dashboard.html` 整体**重写成手机聊天 App**（会话列表 ↔ 聊天线程，2s 轮询消息+自动滚底，输入框发送，右下 ＋ 新建会话弹层选 Agent+工作区，连接指示灯，token 从 URL 注入，纯原生无构建依赖）。`DashboardTab` 远程卡片改为「手机远程访问」**开关**（调 `remoteApi.setAccess` 重启代理绑定 LAN）+ 显示访问 URL/IP/令牌 + 复制；并如实标注"异地需自建隧道，OMNIX 不内置穿透"。
  - **验证**：`cargo check --lib` + `tsc --noEmit` clean；app 重启稳定（1421 监听）。实测端点：无 token → 401；`/agents` → Claude Code/Codex 均 installed；`/conversations`、`/workspaces` → `[]`（该实例 DB 暂无 agent 会话，属正确空数据）。完整聊天链路需用户实际跑一个 Agent 会话 + 手机端实测。QR 暂以 URL+复制替代（二维码留作小后续）。
- **去掉 Cowork（按用户决定）+ 远程补全二维码 & 手机审批。** 用户明确：不需要手动 Cowork 测试台、也不需要 Agent 自主操作电脑，**只要远程手机访问**。
  - **移除 Cowork**：删 `commands/cowork.rs` + `CoworkTab.tsx`；从 `mod.rs`/`lib.rs` 注册、`appRegistry`(条目+图标+MonitorPlay)、`App.tsx`(lazy/preload/render)、`tauri-api`(coworkApi) 全部清掉；`Cargo.toml` 去掉只为截屏加的 `xcap` + `png` 依赖。`cargo check` + `tsc` clean。
  - **(a) 二维码**：装 `qrcode` 前端库；`DashboardTab` 远程卡片在启用且有 URL 时用 `QRCode.toDataURL` 渲染一张二维码（手机同 Wi-Fi 扫码直接打开），旁附 IP/令牌/复制。
  - **(a) 手机审批**：后端新增 `GET /api/remote/pending?conversation_id`（查该会话最新 runtime session 的 `agent_sessions.status=='awaiting_approval'` + 最近 `runtime_events` 里 `kind='approval_requested'` 的 `request_id`/`text`）与 `POST /api/remote/respond {conversation_id, approved}`（解析该审批事件 `metadata_json` 的 `method` + `params.permissions`，调 `runtime_manager.respond_approval`）。`remote_dashboard.html` 聊天页加审批条（标题 + ✓批准/✗拒绝），每 2s 随消息一起轮询 `/pending`。**诚实限制**：结构化审批回传目前仅 Codex 支持（Claude Code 未验证，`respond_approval` 直接返回提示）。
  - **验证**：`cargo check --lib` + `tsc --noEmit` clean；app 重启稳定。实测：`/pending` 无 token→401、带 token→`{pending:false}`（无待审批，正确）；`conversations` 仍 200；`lib.rs`/`tauri-api`/`App.tsx` 中 cowork 引用为 0。
