# Build and output directories
bin/
obj/
dist/
build/
out/
.next/

# Rust / Tauri build artifacts
target/
src-tauri/target/
*.exe
*.msi
*.nsis

# Dependencies
node_modules/
jspm_packages/
web_modules/

# Environment and secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.pem
*.key

# User data directory (contains encrypted keys and SQLite DB)
.omnix/

# IDEs and editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp
*.swo

# OS files
Thumbs.db
ehthumbs.db
Desktop.ini

# Local debug logs
debug.log

# Claude local settings (per-machine, not shared)
.claude/settings.local.json

# Keep log/memory directories but ignore their contents if desired (or keep them versioned)
# If we want to commit the logs to maintain project history:
# logs/tasks/*
# !logs/tasks/.gitkeep
# (We will keep them unignored for now so that the development memory travels with the repository,
# which matches "保证任何新Agent/新模型都能快速接管项目")

# Temporary scratch space for clones/downloads
scratch/

# Reference source (not part of the project)
cherry-studio-ref/


# Antigravity CLI 探针输出（本地临时文件）
agy_out.txt
t.txt

# OMNIX 注入的避坑记忆是本机个人资产（踩过的坑、机器配置、做过的项目），
# 不随开源仓库分发。这两个文件在本项目里只有注入内容，没有手写约定。
CLAUDE.md
AGENTS.md

# 后台任务的 git worktree（本地工作副本，不入库）
.claude/worktrees/
